spring:
  datasource:
    url: jdbc:h2:mem:lg-test
    username: lg
    password: laundryGo!
    hikari:
      maximum-pool-size: 4
  h2:
    console:
      enabled: true

logging:
  level:
    root: info
    com.laundrygo.shorturl.repository: debug